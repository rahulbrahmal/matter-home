<?xml version="1.0" encoding="UTF-8"?>
<!--
  Reference copy of the live agent on kl_2_server, captured from
  ~/Library/LaunchAgents/. __HOME__ stands in for the server's home directory.
  The deploy does not install this file — it is committed so production process
  config is reviewable and can be rebuilt if the server is ever reimaged.

  Note: --protocol http2 (not cloudflared's default QUIC). Keep it that way
  unless UDP/7844 outbound is known to work from the home LAN.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
  <key>Label</key><string>com.matterhome.tunnel</string>
  <key>ProgramArguments</key><array>
    <string>/opt/homebrew/bin/cloudflared</string><string>tunnel</string><string>--protocol</string><string>http2</string><string>run</string><string>matter-home</string>
  </array>
  <key>RunAtLoad</key><true/><key>KeepAlive</key><true/>
  <key>StandardOutPath</key><string>__HOME__/Library/Logs/matterhome/tunnel.log</string>
  <key>StandardErrorPath</key><string>__HOME__/Library/Logs/matterhome/tunnel.log</string>
</dict></plist>
