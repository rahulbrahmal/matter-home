# Copy to .env and fill in. The gateway loads these when started via: node --env-file=.env server.mjs
GW_TOKEN=change-me-to-a-long-random-string   # shared secret: the FE login password AND the API bearer token
MS_URL=ws://localhost:5580/ws                # matter-server websocket
PORT=8788
